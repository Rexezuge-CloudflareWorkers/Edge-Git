# Edge-Git — Testing

Scope: unit + integration tests. Parent index: `../../../AGENTS.md`.

Current thresholds (`vitest.config.mts`): **statements 80 / branches 70 / functions 80 / lines 85** (raised stepwise from 50/40/50/50; measured 2026-09-19: 83.95/75.42/83.17/88.58 — above all four gates). Exclusions: `**/*.test.ts`, `**/*.d.ts`, `**/index.ts`, `**/types.d.ts`, `**/model/**`. Integration in `test/integration/` uses `@cloudflare/vitest-pool-workers` (no V8 coverage — no thresholds there, omitted intentionally). God-file guard: `scripts/check-god-files.mjs` (soft 300 / hard 400 LOC; warn-only, `continue-on-error` in CI).
Raise plan (stepwise, only when green with headroom): 80/70/80/85 → 85/70/85/85 → Otter parity 88.5/76.5/90.5/89.5. Never lower thresholds to make CI pass.

**Covered** (test files exist): error classes + AWS `{Exception:{Type,Message}}` envelope (`test/hardening-envelope.test.ts`: `toErrorType`/`jsonError`/`toErrorBody`, `mapServiceError`/`buildBody`/`failClosed`, `ErrorTranslation|DeserializationUtil` incl. legacy `{error,message}` + plain-text compat, secret-safe logging redaction, `memoizeAsync` rejection retry, `ErrorNormalizer` code preservation, `normalizePath` byte-exactness, `SearchQueries` builders, batched `getTeamRole`, web `lib/api` dual-envelope parsing) + `Container`/`ServiceContext`/`AppConfiguration`/`ConfigurationManager`, `CryptoUtil`/`UUIDUtil`/`TimestampUtil`, pkt-line + v2 fetch/receive builders/parsers + limits, `GitService` pack/ref paths, DAOs (`Repository/User/UserAccessToken/Issue`, `PullRequest` threads/dismiss, `Search` pulls, `Team/Member/Grant`, `AuditLog`, `DeployKey`, `TokenRepoGrant`), services (`Repo/User/Token/Issue`, `PullRequest` threads/dismiss, `SearchService`, `BranchProtectionService` CODEOWNERS quorum, `Star/Watch/Notification`, `Project/Discussion/Wiki/Snippet`, `Team/Org/Release/Audit/Fork/Webhook/Check`), `AccessAuthService` (DEMO/DEV/JWT fallbacks + prod bypass guard + JWK cache + generic JWT errors), request-scope composition (`Tokens` registry + `createRequestScope` memoization + ctor-injected DAO overrides, Otter `createMockDb`/`vi.mock`/`vi.hoisted` pattern), shared i18n (`getBackendStrings` en/zh-CN + fallback + `formatBackendString`; web bundles via `validate_locales` invocation), web `lib/threads` gate parity/grouping (`test/web-threads.test.ts`), worker routes + middleware auth (route sweep covers search/teams/audit/releases/webhooks/checks/social/collab), cron `TaskRegistry` phases, security hardening (`test/security-hardening.test.ts`: SSRF, token expiry, issue caps, rate limit, headers), hardening batch (`rate-limit-hardening`, `security-headers-coop`, `validation-schemas-hardening`, `ssrf-redirect-hardening`, `fetch-handler-hardening`, `middleware-error-masking`, `audit-query-parser`, `AuditLifecycle` + `DeployKeyLifecycle` integration), fail-closed token grants (`token-grant-failclosed`: scoped-grant rollback, list enrichment throw, rotate case-normalize) + error-mask sweep (`error-masking-sweep`), DO read path (`repo-read-rpc-hardening`: `RepoReadRpc` 98.8%, `RepoMove`, `ReleaseAssetStore`, `RepoWorker` routing), checks + scheduled (`check-runner-hardening`, `scheduled-tasks-fill`: sandbox limits, allowHosts, backfill/sweeper/prune/stale/mirror, `fetchAdapter` redirect budget), git internals (`git-service-fill`: `RefService`/`GitService`/`TreeReader`/`PackCollector` + `DofsFsAdapter` via transpile-injected fake; `write-path-exists`: tree-walk existence), route fill (`route-fill-hardening`: Git/Project/Org/Team routes, `PublicViewerResolver`, `SocialEmit`, `SecuritySettingsDAO`, release assets), integration `TeamLifecycle` + `PullLifecycle` + `GitPushLifecycle` (teams CRUD/members/grants + team-mediated git, PR happy path, real pack negotiation/mutation gates).

**Still thin**: `RepoWorker` facade (26% — routing covered via prototype injection; pack/fetch internals need a DO harness), `DofsFsAdapter` still 0 in V8 (logic covered via transpile-injected fake in `git-service-fill`, but the real module pulls `cloudflare:*` in node), SPA components (helper-level web tests only: `lib/threads`, `lib/permissions` parity, `lib/api` helpers — no component tests yet), routes `GitRoutes` (32%), `TriggerChecks` (32%), `ReleaseRoutes` (45%), `SnippetRoutes` (47%), `FetchHandler` (47%), `SocialEmit` (51%), `UserRoutes` (53%), `CheckRoutes` (59%), `TeamRoutes` (62%), `GitService` remainder (64%). Workerd quirk to remember: isomorphic-git `readBlob` misses leave dangling rejections the pool surfaces as unhandled (fails exit code) — fixed by tree-walk existence (`WriteService.pathExistsAt`) + list-then-read (`CodeownerHelpers.readCodeownerRules`); keep exception-driven blob reads out of hot paths.

**Mock patterns**:

- DAOs/services: in-memory fake D1 (`createFakeDb`/`createDaoFakeDb` in `test/services.test.ts`/`test/dao.test.ts`) implementing `prepare().bind().first/all/run` with per-table arrays; assert via state, not `vi.mock`.
- PAT hashing: real `TokenService.hashToken` (`sha256 edge-git-pat:`) with ephemeral random tokens; expiry via `TimestampUtil` arithmetic.
- Access auth: stub env (`DEV_AUTH_EMAIL`/`DEMO_MODE`) or `cf-access-jwt-assertion` header with mocked `jose`; never trust `Cf-Access-Authenticated-User-Email`.
- Integration: `test/integration/vitest.config.mts` + `wrangler.test.jsonc` pool, shared `__INTEGRATION_MIGRATION_SQL__` seeding; `beforeEach` + fresh fakes, no cross-test state. `helpers/setup.ts` (`setupIntegrationTest`/`ensureUser`/`seedRepo`) + `helpers/migrations.ts` (`splitSql`) back `api/RepoCrud|TokenLifecycle|IssueLifecycle.int.test.ts` (real D1 via `SELF.fetch`, `DEV_AUTH_EMAIL`). Upstream fix: `dofs@0.1.0` ships an uncompiled `@Dofs(...)` decorator example in `dist/index.js` that the pool workerd rejects with a bare `SyntaxError`; stripped via `patches/dofs@0.1.0.patch` (`pnpm-workspace.yaml` `patchedDependencies`) — re-check on every `dofs` bump, drop the patch once upstream ships compiled dist.
