# Edge-Git — Testing

Scope: unit + integration tests. Parent index: `../../../AGENTS.md`.

Current thresholds (`vitest.config.mts`): **statements 85 / branches 75 / functions 85 / lines 88** (raised stepwise from 50/40/50/50 via 80/70/80/85, 82/72/82/87, and 83/74/82/88; measured post-hardening: 85.74/76.16/86.67/90.41 — above all four gates). Exclusions: `**/*.test.{ts,tsx}`, `**/*.d.ts`, `**/index.ts`, `**/types.d.ts`, `**/model/**`. Integration in `test/integration/` uses `@cloudflare/vitest-pool-workers` (no V8 coverage — no thresholds there, omitted intentionally). God-file guard: `scripts/check-god-files.mjs` (soft 300 / hard 400 LOC; hard fail in CI, wired into `pnpm run checks`).
Raise plan (stepwise, only when green with headroom): 85/75/85/88 → Otter parity 88.5/76.5/90.5/89.5. Never lower thresholds to make CI pass.

**Covered** (test files exist): error classes + AWS `{Exception:{Type,Message}}` envelope (`test/hardening-envelope.test.ts`: `toErrorType`/`jsonError`/`toErrorBody`, `mapServiceError`/`buildBody`/`failClosed`, `ErrorTranslation|DeserializationUtil` incl. legacy `{error,message}` + plain-text compat, secret-safe logging redaction, `memoizeAsync` rejection retry, `ErrorNormalizer` code preservation, `normalizePath` byte-exactness, `SearchQueries` builders, batched `getTeamRole`, web `lib/api` dual-envelope parsing) + pure policy modules (`test/ref-validation.test.ts`: `RefValidation` classify/OID/branch-ref + `applyRefUpdates` custom-gitdir regression; `test/release-validation.test.ts`: `ReleaseValidation` tag/asset/MIME/sha256 + `ReleaseService.isValidTagName` parity) + `Container`/`ServiceContext`/`AppConfiguration`/`ConfigurationManager`, `CryptoUtil`/`UUIDUtil`/`TimestampUtil`, pkt-line + v2 fetch/receive builders/parsers + limits, `GitService` pack/ref paths, DAOs (`Repository/User/UserAccessToken/Issue`, `PullRequest` threads/dismiss, `Search` pulls, `Team/Member/Grant`, `AuditLog`, `DeployKey`, `TokenRepoGrant`), services (`Repo/User/Token/Issue`, `PullRequest` threads/dismiss, `SearchService`, `BranchProtectionService` CODEOWNERS quorum, `Star/Watch/Notification`, `Project/Discussion/Wiki/Snippet`, `Team/Org/Release/Audit/Fork/Webhook/Check`), `AccessAuthService` (DEMO/DEV/JWT fallbacks + prod bypass guard + JWK cache + generic JWT errors), request-scope composition (`Tokens` registry + `createRequestScope` memoization + ctor-injected DAO overrides, Otter `createMockDb`/`vi.mock`/`vi.hoisted` pattern), shared i18n (`getBackendStrings` 12 locales + fallback + `formatBackendString`; web bundles via `validate_locales` invocation), web `lib/threads` gate parity/grouping (`test/web-threads.test.ts`), worker routes + middleware auth (route sweep covers search/teams/audit/releases/webhooks/checks/social/collab), cron `TaskRegistry` phases, security hardening (`test/security-hardening.test.ts`: SSRF, token expiry, issue caps, rate limit, headers), hardening batch (`rate-limit-hardening`, `security-headers-coop`, `validation-schemas-hardening`, `ssrf-redirect-hardening`, `fetch-handler-hardening`, `middleware-error-masking`, `audit-query-parser`, `AuditLifecycle` + `DeployKeyLifecycle` integration), fail-closed token grants (`token-grant-failclosed`: scoped-grant rollback, list enrichment throw, rotate case-normalize) + error-mask sweep (`error-masking-sweep`), DO read path (`repo-read-rpc-hardening`: `RepoReadRpc` 98.8%, `RepoMove`, `ReleaseAssetStore`, `RepoWorker` routing), checks + scheduled (`check-runner-hardening`, `scheduled-tasks-fill`: sandbox limits, allowHosts, backfill/sweeper/prune/stale/mirror, `fetchAdapter` redirect budget), git internals (`git-service-fill`: `RefService`/`GitService`/`TreeReader`/`PackCollector` + `DofsFsAdapter` via transpile-injected fake; `write-path-exists`: tree-walk existence), route fill (`route-fill-hardening`: Git/Project/Org/Team routes, `PublicViewerResolver`, `SocialEmit`, `SecuritySettingsDAO`, release assets), hardening cleanup (`test/harden-cleanup.test.ts`: `BranchValidation`/`SlugValidation`/`RepoFullName` layering, `PullReviewGate`, `WebhookRetryPolicy`, `RefParsers`, `PackLimits`, `GitCache` composition, `RealtimePolicy`, fail-closed `numberAllocator`, `AuditObserverRegistry` composition wiring), integration `TeamLifecycle` + `PullLifecycle` + `GitPushLifecycle` (teams CRUD/members/grants + team-mediated git, PR happy path, real pack negotiation/mutation gates).

**Big-bang hardening** (`test/hardening-bigbang.test.ts`, 14 tests): `AccessAuthService` single-throw chain (JWT fail-soft → `ctx.access` fallback reachable, no double JWK verify), pkt-line `Error`-not-`TypeError` + receive/fetch NaN length fail-closed, `validateFetchRequestCounts` injected-clock future-skew, `AbstractPruningTask` cutoff/swallow semantics + task names, composition split (`daoBindings`/`serviceBindings` wiring), `ContentLimits`/`RetentionLimits` facade parity, `gitAuthForRepo` fail-closed 503 on `DatabaseError` vs 401 on bad token vs success result.

**Still thin**: `RepoWorker` facade (routing covered via prototype injection; pack/fetch internals need a DO harness), `DofsFsAdapter` still 0 in V8 (logic covered via transpile-injected fake in `git-service-fill`, but the real module pulls `cloudflare:*` in node), SPA components (first jsdom test `test/web-card.test.tsx` + slice helpers in `test/web-slice4.test.ts`; no full view tests yet), routes `PullMergeRoutes` (40% funcs), `CodeownerHelpers` (45%), `UserRoutes` (28% funcs — catch closures need failure injection), `SearchRoutes` (57%), `SocialEmit` (55% — `.catch` fan-out closures), `GitRoutes` (now 88% stmts / 93% funcs after `test/git-push-paths.test.ts`), `ReleaseRoutes` (69%), `TeamRoutes` (67%), `CheckRunnerWorker` (47%), `ImportRunner`/`MirrorRunner` (happy paths in `test/transfer-runners-e2e.test.ts`; residual `.catch` closures). Zero-percent `PullRoutes` is an unmeasurable pure re-export barrel (zero counted statements — no gate impact; the `repoStub|checkStub|realtimeStub` shims were collapsed into `doStubs.ts`). Workerd quirk to remember: isomorphic-git `readBlob` misses leave dangling rejections the pool surfaces as unhandled (fails exit code) — fixed by tree-walk existence (`WriteService.pathExistsAt`) + list-then-read (`CodeownerHelpers.readCodeownerRules`); keep exception-driven blob reads out of hot paths.

**Mock patterns**:

- DAOs/services: in-memory fake D1 (`createFakeDb`/`createDaoFakeDb` in `test/services.test.ts`/`test/dao.test.ts`) implementing `prepare().bind().first/all/run` with per-table arrays; assert via state, not `vi.mock`.
- PAT hashing: real `TokenService.hashToken` (`sha256 edge-git-pat:`) with ephemeral random tokens; expiry via `TimestampUtil` arithmetic.
- Access auth: stub env (`DEV_AUTH_EMAIL`/`DEMO_MODE`) or `cf-access-jwt-assertion` header with mocked `jose`; never trust `Cf-Access-Authenticated-User-Email`.
- Integration: `test/integration/vitest.config.mts` + `wrangler.test.jsonc` pool, shared `__INTEGRATION_MIGRATION_SQL__` seeding; `beforeEach` + fresh fakes, no cross-test state. `helpers/setup.ts` (`setupIntegrationTest`/`ensureUser`/`seedRepo`) + `helpers/migrations.ts` (`splitSql`) back `api/RepoCrud|TokenLifecycle|IssueLifecycle.int.test.ts` (real D1 via `SELF.fetch`, `DEV_AUTH_EMAIL`). Upstream fix: `dofs@0.1.0` ships an uncompiled `@Dofs(...)` decorator example in `dist/index.js` that the pool workerd rejects with a bare `SyntaxError`; stripped via `patches/dofs@0.1.0.patch` (`pnpm-workspace.yaml` `patchedDependencies`) — re-check on every `dofs` bump, drop the patch once upstream ships compiled dist.
